# vigor-runtime

The VIGOR runtime: orchestrator, budget enforcement, in-process echo backend for tests, CLI.

Depends on `vigor-core` only. Adapters and agent backends are plugged in at runtime.

Commands:

```bash
uv run vigor run --help
uv run vigor demo
```
