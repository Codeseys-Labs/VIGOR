# vigor-core

Core schemas, abstract interfaces, run archive, scoring/adjudication, and frontier logic for the VIGOR framework.

This package has no runtime dependencies besides Pydantic v2. It is safe to import from adapters and backends.

See the top-level `README.md` and `docs/` for framework context.
