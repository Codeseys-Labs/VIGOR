[project]
name = "vigor-adapter-cad"
version = "0.1.0"
description = "VIGOR domain adapter for first-slice parametric CAD via OpenSCAD source generation."
readme = "README.md"
requires-python = ">=3.11"
license = { file = "../../LICENSE" }
authors = [{ name = "The VIGOR Authors" }]

dependencies = ["pydantic>=2.8", "vigor-core"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/vigor_adapter_cad"]

[tool.uv.sources]
vigor-core = { workspace = true }
