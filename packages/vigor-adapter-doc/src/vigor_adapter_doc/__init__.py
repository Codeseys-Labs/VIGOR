"""VIGOR structured-documents adapter (skeleton).

The IR schemas, adapter class, compile path, reviewers, ``apply_patch``,
and export bundle land in follow-up Seeds tasks rooted at VIGOR-c916.
This module currently exposes only the package version so the workspace
build, lint, and type-check pass while the ADR-0019 Phase 7 work is
broken into vertical slices.
"""

__all__: list[str] = []
__version__ = "0.1.0"
